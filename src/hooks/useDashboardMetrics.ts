/**
 * @deprecated Use useDashboardData instead - it uses client-side Firebase directly
 * This hook used server-side Firebase Admin which requires service account credentials
 */
export { useDashboardData as useDashboardMetrics } from './useDashboardData';
